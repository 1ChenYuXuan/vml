
some terminology...
- location 2bytesq

# VML
## VM
In short, the bytes code can expressed as vector of a easy structure. It look like: head cmd arg1 arg2 ... The cmds have 256 bytes at most becase the head given a number to get the length of the cmd.
It uses **functions.hpp** to do something.
- mov   mov bytes location


