
def lexer(codes):
    tsplit_lines: list[str] = code.split('\n')
    split_lines: list[str] = []
    for code in tspilt_codes:
        code = code.strip()
        if code:
            split_lines.append(code)


